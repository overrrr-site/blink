# カルテページ実装指示書

## 概要

ペット事業者向けカルテ記録画面の実装仕様書。トリミング・幼稚園（連絡帳）・ホテルの3業態に対応し、共通コンポーネントと業態固有コンポーネントを組み合わせた設計。

**参照ワイヤーフレーム**: `pet-platform-wireframes-v8.jsx`

---

## 1. デザイントークン

### カラーパレット

```typescript
const colors = {
  // 基本色
  bg: '#F8FAFC',              // 背景
  surface: '#FFFFFF',          // カード背景
  surfaceHover: '#F1F5F9',     // ホバー・入力欄背景
  
  // テキスト
  textPrimary: '#0F172A',      // 見出し・本文
  textSecondary: '#475569',    // サブテキスト
  textTertiary: '#94A3B8',     // プレースホルダー
  
  // ボーダー
  border: '#E2E8F0',           // 通常
  borderLight: '#F1F5F9',      // 薄い
  
  // アクセント
  primary: '#3B82F6',          // 青（汎用）
  primaryLight: '#EFF6FF',
  success: '#10B981',          // 緑（成功）
  successLight: '#ECFDF5',
  warning: '#F59E0B',          // 黄（警告）
  warningLight: '#FFFBEB',
  danger: '#EF4444',           // 赤（エラー・気になる箇所）
  dangerLight: '#FEF2F2',
  
  // 業態別
  grooming: '#8B5CF6',         // トリミング（紫）
  groomingLight: '#F5F3FF',
  groomingPale: '#FAF5FF',
  daycare: '#F97316',          // 幼稚園（オレンジ）
  daycareLight: '#FFF7ED',
  daycarePale: '#FFFAF5',
  hotel: '#06B6D4',            // ホテル（シアン）
  hotelLight: '#ECFEFF',
  hotelPale: '#F0FDFF',
  
  // AI機能
  ai: '#6366F1',               // インディゴ
  aiLight: '#EEF2FF',
}
```

### シャドウ

```typescript
const shadows = {
  xs: '0 1px 2px rgba(0, 0, 0, 0.04)',
  sm: '0 1px 3px rgba(0, 0, 0, 0.06), 0 1px 2px rgba(0, 0, 0, 0.04)',
  md: '0 4px 6px -1px rgba(0, 0, 0, 0.08), 0 2px 4px -1px rgba(0, 0, 0, 0.04)',
  lg: '0 10px 15px -3px rgba(0, 0, 0, 0.08), 0 4px 6px -2px rgba(0, 0, 0, 0.04)',
}
```

### 角丸

```typescript
const radius = {
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '20px',
  full: '9999px',
}
```

---

## 2. 画面構成

### ページレイアウト

```
┌─────────────────────────────────────┐
│ ヘッダー（sticky）                    │
├─────────────────────────────────────┤
│ ペット情報カード                      │
├─────────────────────────────────────┤
│ 必須セクション                        │
│ ├ 業態固有フォーム                    │
│ ├ 写真フォーム                        │
│ └ 報告文フォーム                      │
├─────────────────────────────────────┤
│ 任意セクション（折りたたみ）            │
│ ├ 体調・様子                          │
│ └ 健康チェック                        │
└─────────────────────────────────────┘
```

### ヘッダー

| 要素 | 仕様 |
|------|------|
| 戻るボタン | 40x40px、`surfaceHover`背景、`ChevronLeft`アイコン |
| タイトル | ペット名（16px/700）、業態名（12px/400） |
| 設定ボタン | 40x40px、`surfaceHover`背景、`Settings`アイコン |
| 保存ボタン | セカンダリ（グレーボーダー） |
| 共有ボタン | プライマリ（業態アクセントカラー、グラデーション） |

```typescript
// 共有ボタンのスタイル
background: `linear-gradient(135deg, ${accentColor} 0%, ${accentColor}DD 100%)`
boxShadow: `0 2px 8px ${accentColor}40`
```

---

## 3. 必須セクション

### セクションコンポーネント（RequiredSection）

```typescript
interface RequiredSectionProps {
  title: string;
  children: ReactNode;
}
```

**スタイル仕様**:
- タイトル: 15px/700、`textPrimary`
- 「必須」バッジ: 10px/700、`danger`色、`dangerLight`背景、pill形状
- コンテンツ: 白背景カード、`shadow-sm`、`radius-lg`、padding 16px

### 3.1 カットスタイルフォーム（トリミング専用）

**犬シルエット図**
- サイズ: 高さ180px
- 背景: `groomingPale`
- ボーダー: 2px solid `grooming`（40%透過）
- SVGシルエット: 70%サイズ、20%透過

**部位ボタン**

| 部位 | 位置（%） |
|------|----------|
| 頭 | x:50, y:12 |
| 顔 | x:50, y:28 |
| 耳 | x:28, y:10 |
| 体 | x:50, y:50 |
| しっぽ | x:85, y:38 |
| 前足 | x:35, y:78 |
| 後足 | x:68, y:78 |
| お尻 | x:75, y:55 |

**ボタンスタイル**:
- 未選択: 白背景、`grooming`ボーダー・文字
- 選択時: `grooming`背景、白文字、シャドウ追加

**選択後の入力欄**
```
[体  ] [10mm                    ] [×]
[顔  ] [テディベア              ] [×]
```
- ラベル: 固定幅56px、`grooming`背景、白文字、中央揃え
- 入力欄: flex:1、`surfaceHover`背景
- 削除ボタン: 28x28px

### 3.2 今日の活動フォーム（幼稚園専用）

**活動オプション**（トグル選択）

| value | label |
|-------|-------|
| freeplay | 🎾 フリープレイ |
| training | 📚 トレーニング |
| walk | 🚶 お散歩 |
| nap | 😴 お昼寝 |
| socialization | 🐕 社会化 |

**スタイル**:
- 未選択: 白背景、`border`ボーダー、`textSecondary`文字
- 選択時: `daycareLight`背景、`daycare`ボーダー・文字

### 3.3 宿泊情報フォーム（ホテル専用）

**表示項目**:
- チェックイン日時
- チェックアウト予定日時

**スタイル**:
- 背景: `hotelPale`
- ボーダー: `hotel`（30%透過）
- アイコン: `Moon`

### 3.4 写真フォーム（全業態共通）

**仕上がり写真**
- サムネイル: 80x80px
- 追加ボタン: 80x80px、破線ボーダー（`border`色）、`Plus`アイコン

**気になる箇所（トリミングのみ）**
- 背景: `dangerLight`
- サムネイル: 90x90px
- アノテーション: 赤い円（32x32px、3pxボーダー）
- ラベル: 下部に黒背景オーバーレイ

### 3.5 報告文フォーム（全業態共通）

**内部メモ**
- ラベル: 「内部メモ（飼い主に非公開）」
- 高さ: 72px
- 背景: `surfaceHover`

**飼い主への報告文**
- 高さ: min 100px（リサイズ可能）
- 背景: 白

---

## 4. 任意セクション

### セクションコンポーネント（OptionalSection）

```typescript
interface OptionalSectionProps {
  title: string;
  collapsed: boolean;
  onToggle: () => void;
  children: ReactNode;
}
```

**スタイル仕様**:
- 折りたたみ時: ボーダー薄く、背景透明
- 展開時: `shadow-xs`、白背景
- タイトル: 14px/600
- アイコン: `ChevronDown`（閉）/ `ChevronUp`（開）

### 4.1 体調・様子フォーム

**オプション**（単一選択）

| value | label |
|-------|-------|
| excellent | 😆 絶好調 |
| good | 😊 元気 |
| normal | 😐 普通 |
| tired | 😴 疲れ気味 |
| observe | 🤒 要観察 |

### 4.2 健康チェックフォーム

**体重グラフ（トリミングのみ）**
- SVG折れ線グラフ
- 高さ: 56px
- 最新ポイント: やや大きく（r=4）
- 体重変化バッジ: 増加→`warning`、減少→`success`

**健康項目**（2カラムグリッド）

| key | label | options |
|-----|-------|---------|
| ears | 耳 | きれい / 汚れ / 赤み |
| nails | 爪 | 普通 / 長め / 伸びすぎ |
| skin | 皮膚 | 良好 / 赤み / 湿疹 |
| teeth | 歯 | 良好 / 汚れ / 歯石 |

---

## 5. AIアシスタント機能

### インライン提案コンポーネント（AISuggestion）

```typescript
interface AISuggestionProps {
  message: string;           // 提案メッセージ
  preview?: ReactNode;       // プレビュー内容
  actionLabel: string;       // アクションボタンラベル
  onAction: () => void;      // アクション実行時
  onDismiss: () => void;     // スキップ時
  applied?: boolean;         // 適用済みフラグ
  variant?: 'default' | 'warning' | 'success';
}
```

**レイアウト**:
```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  ← カラーライン（3px）
┃ 🔮 メッセージ                  [×] ┃
┃    ┌───────────────────────┐     ┃
┃    │ プレビュー内容         │     ┃
┃    └───────────────────────┘     ┃
┃    [アクションボタン] [スキップ]    ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

**スタイル**:
- 上部ライン: 3px、グラデーション
- AIアイコン: 28x28px、グラデーション背景、シャドウ
- アクションボタン: グラデーション背景、シャドウ

**バリエーション**:

| variant | line/bg色 |
|---------|----------|
| default | `ai` / `aiLight` |
| warning | `warning` / `warningLight` |
| success | `success` / `successLight` |

### 提案タイプ

| type | トリガー | メッセージ例 | アクション |
|------|---------|-------------|-----------|
| photo-concern | 写真追加時 | 「写真2に赤みを検出しました」 | 気になる箇所に追加 |
| health-history | 健康チェック入力時 | 「耳の汚れが2回連続しています」 | 報告文に追記 |
| report-draft | 報告文が空の時 | 「入力内容から報告文を作成しました」 | 下書きを使用 |

### AI設定画面

**設定項目**:

| 設定 | デフォルト | 説明 |
|------|-----------|------|
| AIアシスタント | ON | 提案機能のON/OFF |
| 自店舗のAI改善に使用 | ON | 店舗内データで学習 |
| サービス全体の改善に貢献 | OFF | 匿名化データを共有 |

**使用しない範囲（明示）**:
- 外部AI（OpenAI等）のモデル学習
- 第三者への提供・販売
- 広告・マーケティング目的

---

## 6. データ構造

### カルテレコード

```typescript
interface Record {
  id: string;
  petId: string;
  recordType: 'grooming' | 'daycare' | 'hotel';
  date: string;  // ISO 8601
  
  // 業態固有
  grooming?: GroomingData;
  daycare?: DaycareData;
  hotel?: HotelData;
  
  // 共通
  photos: PhotosData;
  notes: NotesData;
  condition?: ConditionData;
  healthCheck?: HealthCheckData;
  
  // メタ
  status: 'draft' | 'saved' | 'shared';
  createdAt: string;
  updatedAt: string;
  sharedAt?: string;
}
```

### 業態固有データ

```typescript
interface GroomingData {
  selectedParts: string[];  // ['body', 'face', ...]
  partNotes: Record<string, string>;  // { body: '10mm', face: 'テディベア' }
}

interface DaycareData {
  activities: string[];  // ['freeplay', 'training', ...]
  training?: {
    items: Record<string, 'done' | 'almost' | 'not_done'>;
  };
}

interface HotelData {
  checkIn: string;
  checkOutScheduled: string;
  nights: number;
  specialCare?: string;
}
```

### 共通データ

```typescript
interface PhotosData {
  regular: Photo[];
  concerns?: ConcernPhoto[];  // トリミングのみ
}

interface Photo {
  id: string;
  url: string;
  uploadedAt: string;
}

interface ConcernPhoto extends Photo {
  annotation?: { x: number; y: number };  // %座標
  label?: string;
}

interface NotesData {
  internalNotes?: string;
  reportText?: string;
  aiGenerated?: boolean;
}

interface ConditionData {
  overall: 'excellent' | 'good' | 'normal' | 'tired' | 'observe';
}

interface HealthCheckData {
  weight?: number;
  ears?: string;
  nails?: string;
  skin?: string;
  teeth?: string;
}
```

---

## 7. API エンドポイント

### カルテ

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/records/:id` | カルテ取得 |
| POST | `/records` | カルテ作成 |
| PUT | `/records/:id` | カルテ更新 |
| POST | `/records/:id/share` | 飼い主に共有 |
| GET | `/pets/:petId/records` | ペットのカルテ一覧 |
| GET | `/pets/:petId/records/latest` | 最新カルテ取得（前回コピー用） |

### 写真

| Method | Endpoint | 説明 |
|--------|----------|------|
| POST | `/records/:id/photos` | 写真アップロード |
| DELETE | `/records/:id/photos/:photoId` | 写真削除 |

### AI

| Method | Endpoint | 説明 |
|--------|----------|------|
| POST | `/ai/analyze-photo` | 写真分析（気になる箇所検出） |
| POST | `/ai/generate-report` | 報告文生成 |
| GET | `/ai/suggestions/:recordId` | 提案取得 |

---

## 8. 状態管理

### ローカルState

```typescript
// フォームデータ
const [groomingData, setGroomingData] = useState<GroomingData>();
const [daycareData, setDaycareData] = useState<DaycareData>();
const [hotelData, setHotelData] = useState<HotelData>();
const [photos, setPhotos] = useState<PhotosData>();
const [notes, setNotes] = useState<NotesData>();
const [condition, setCondition] = useState<ConditionData>();
const [healthCheck, setHealthCheck] = useState<HealthCheckData>();

// UI状態
const [collapsed, setCollapsed] = useState({
  condition: true,
  health: true,
});

// AI提案状態
const [aiSuggestions, setAiSuggestions] = useState<Record<string, {
  type: string;
  dismissed: boolean;
  applied: boolean;
}>>();
```

### グローバルState（要検討）

- ユーザー設定（AI設定含む）
- ペット情報キャッシュ
- 体重履歴キャッシュ

---

## 9. インタラクション

### 保存フロー

```
1. ユーザーが「保存」をタップ
2. バリデーション（必須項目チェック）
3. API: PUT /records/:id
4. 成功 → トースト表示「保存しました」
5. 失敗 → エラートースト
```

### 共有フロー

```
1. ユーザーが「共有」をタップ
2. バリデーション（必須項目 + 報告文チェック）
3. 確認ダイアログ「飼い主に送信しますか？」
4. API: POST /records/:id/share
5. 成功 → トースト表示「送信しました」
6. 失敗 → エラートースト
```

### 前回コピーフロー

```
1. ユーザーが「前回コピー」をタップ
2. API: GET /pets/:petId/records/latest
3. フォームにデータを展開
4. AI提案: 差分があれば表示
```

### AI提案フロー

```
1. トリガー発生（写真追加、入力変更など）
2. API: POST /ai/analyze-photo または GET /ai/suggestions
3. 提案をインライン表示
4. ユーザーがアクション or スキップ
5. アクション → データ更新、「適用しました」表示（2秒）
6. スキップ → 提案を非表示
```

---

## 10. バリデーション

### 必須項目

| 業態 | 必須項目 |
|------|---------|
| トリミング | カットスタイル（1部位以上）、写真（1枚以上）、報告文 |
| 幼稚園 | 活動（1つ以上）、写真（1枚以上）、報告文 |
| ホテル | 宿泊情報、写真（1枚以上）、報告文 |

### 共有時の追加チェック

- 報告文が空でないこと
- 報告文が最低10文字以上（推奨）

---

## 11. アクセシビリティ

- フォーカス可能な要素には適切なtabIndex
- ボタンには適切なaria-label
- 色だけに依存しない情報伝達（アイコン併用）
- タップターゲットは最低44x44px

---

## 12. パフォーマンス

- 写真は即時アップロード（バックグラウンド）
- 自動保存（30秒ごと or 入力変更後5秒）
- 画像の遅延読み込み
- 体重履歴はキャッシュ

---

## 付録: コンポーネント一覧

| コンポーネント | 用途 | Props |
|--------------|------|-------|
| `RequiredSection` | 必須セクションのラッパー | title, children |
| `OptionalSection` | 任意セクションのラッパー | title, collapsed, onToggle, children |
| `CutDetailsForm` | カットスタイル入力 | data, onChange |
| `DaycareActivitiesForm` | 活動選択 | data, onChange |
| `HotelStayInfoForm` | 宿泊情報表示 | data |
| `PhotosForm` | 写真管理 | data, onChange, showConcerns, aiSuggestion, onAISuggestionAction, onAISuggestionDismiss |
| `NotesForm` | メモ・報告文 | data, onChange, photos, recordType, groomingData, aiSuggestion, onAISuggestionAction, onAISuggestionDismiss |
| `ConditionForm` | 体調選択 | data, onChange |
| `HealthCheckForm` | 健康チェック | data, onChange, showWeightGraph, weightHistory, aiSuggestion, onAISuggestionAction, onAISuggestionDismiss |
| `AISuggestion` | AI提案表示 | message, preview, actionLabel, onAction, onDismiss, applied, variant |
| `AISettingsScreen` | AI設定画面 | onClose |
